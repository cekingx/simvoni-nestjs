export enum ElectionStatusEnum {
  draft = 1,
  readyToDeploy = 2,
  deployed = 3,
  started = 4,
  ended = 5,
  rejected = 6,
}

export enum ParticipationEnum {
  waiting_approval = 1,
  accepted = 2,
  voted = 3,
  rejected = 4,
}
